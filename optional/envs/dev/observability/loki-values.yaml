deploymentMode: SingleBinary

singleBinary:
  replicas: 1
  persistence:
    enabled: false

# ✅ validate.yaml 에서 “singleBinary & simpleScalable 둘 다 replicas>0”로 터지는 것 방지
# (차트 버전에 따라 이 블록을 보거나, 아래 read/write/backend 블록을 보는 경우가 있어서 둘 다 0으로 고정)
simpleScalable:
  replicas:
    backend: 0
    read: 0
    write: 0

read:
  replicas: 0
write:
  replicas: 0
backend:
  replicas: 0

loki:
  auth_enabled: false

  # ✅ 핵심: chart가 config.yaml을 만들 때 이 structuredConfig가 가장 확실하게 먹습니다.
  structuredConfig:
    auth_enabled: false

    # 기존 CM에 있던 common.* 형태로 생성되게
    common:
      path_prefix: /var/loki
      replication_factor: 1
      storage:
        filesystem:
          chunks_directory: /var/loki/chunks
          rules_directory: /var/loki/rules

    # ✅ 바로 이게 CrashLoop 원인 해결 포인트
    limits_config:
      allow_structured_metadata: false

      # (옵션) 기존 CM 기본값들 — 필요 없으면 지워도 되지만, 안전하게 유지
      reject_old_samples: true
      reject_old_samples_max_age: 168h
      max_cache_freshness_per_query: 10m
      query_timeout: 300s
      split_queries_by_interval: 15m
      volume_enabled: true

    schema_config:
      configs:
        - from: "2024-01-01"
          store: boltdb-shipper
          object_store: filesystem
          schema: v12
          index:
            prefix: loki_index_
            period: 24h

    storage_config:
      boltdb_shipper:
        active_index_directory: /var/loki/index
        cache_location: /var/loki/cache
        shared_store: filesystem
      filesystem:
        directory: /var/loki/chunks

    compactor:
      working_directory: /var/loki/compactor
      shared_store: filesystem

    # single binary면 사실상 의미 없어서 비워둠
    memberlist:
      join_members: []

    server:
      http_listen_port: 3100
      grpc_listen_port: 9095

chunksCache:
  enabled: false
resultsCache:
  enabled: false

gateway:
  enabled: true
  service:
    type: ClusterIP
  ingress:
    enabled: true
    ingressClassName: nginx
    hosts:
      - host: loki-dev.local
        paths:
          - path: /
            pathType: Prefix
    tls: []

