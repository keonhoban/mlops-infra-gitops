apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  # bootstrap (cluster-level / namespace foundation)
  - namespaces.yaml

  # secrets distribution (sealed-secrets manifests)
  - sealed-secrets

  # feature-store-lite contract & airflow access (core support)
  - feature-store/feature-store-cm.yaml
  - feature-store/airflow-kpo-rbac.yaml

